# Bug Fix Plan: [BUG-NNN]

**Branch**: `[branch-name]` | **Date**: [DATE] | **Bug**: [link]

**Input**: Bug report from `bugs/[BUG-NNN-YYYY-MM-DD-slug].md`

**Note**: This template is filled in by the `speckit.bug-debug.plan` command.

## Summary

[Root cause in one sentence + chosen fix approach from investigation's suggested approaches]

## Bug Context

<!--
  Populated from the investigation findings. Do not fabricate — copy directly
  from the ## Investigation section of the bug report.
-->

**Root Cause**: [precise description of the defect]

**Location**: `file:line`

**Causal Chain**: [observed symptom] ← [proximate cause] ← [root cause]

**Pattern Sweep**: [N occurrences found at: file:line, ... | none found]

## Technical Context

<!--
  ACTION REQUIRED: Fill in the concrete details for this bug's fix context.
  Mark unknowns as "NEEDS CLARIFICATION".
-->

**Language/Version**: [e.g., TypeScript 5.x, Python 3.11 or NEEDS CLARIFICATION]

**Primary Files**: [files that will change, e.g., src/auth/middleware.ts, tests/auth.test.ts]

**Dependencies**: [relevant libs/modules involved in the fix, e.g., jsonwebtoken, express]

**Testing**: [test framework and how to run, e.g., jest — `npm test` or NEEDS CLARIFICATION]

**Target Environment**: [e.g., Node 20, browser, both or NEEDS CLARIFICATION]

**Constraints**: [any performance, compatibility, or deployment constraints or N/A]

## Constitution Check

*GATE: Must pass before fix design. Re-check after fix scope is finalized.*

[Gates determined based on `.specify/memory/constitution.md`. List each relevant principle and whether this fix complies. ERROR if violation is unjustified.]

## Fix Scope

### Primary Fix

**File**: `file:line`

**Change**: [What changes and exactly why — reference root cause]

**Approach chosen**: [Which of the investigation's suggested approaches, and why over alternatives]

### Pattern Sweep Fixes

<!--
  One row per additional occurrence found in the pattern sweep.
  Remove table if sweep found no occurrences.
-->

| File | Line | Fix |
|------|------|-----|
| `path/to/file.ext` | 42 | [brief description of change] |

### Prevention

**Regression test**: [What to assert, which test file, what the test proves]

**Spec clarification** (if applicable): [Spec section to amend and what gap to fill — omit if investigation found no spec gap]

**Guard / lint rule** (if applicable): [Rule description and location — only if investigation or plan explicitly calls for one]

## Project Structure

### Documentation (this bug)

```text
bugs/
├── [BUG-NNN-YYYY-MM-DD-slug].md    # bug report
├── [BUG-NNN]-plan.md               # this file
└── [BUG-NNN]-tasks.md              # generated by speckit.bug-debug.tasks
```

### Source Files Changed

<!--
  ACTION REQUIRED: Replace with the concrete file tree for this fix.
  Show only files that will actually change.
-->

```text
src/
└── [affected files]

tests/
└── [affected test files]
```

**Structure Decision**: [Confirm the layout above matches the real change scope]

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|--------------------------------------|
| [e.g., touches 3 modules] | [this bug spans auth + session + token layers] | [single-module fix would leave causal chain incomplete] |
